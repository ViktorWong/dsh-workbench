# DSH package family resolves siblings via flat node_modules (their npm
# packages expect hoisted layout); also required for electron-builder to
# collect a complete tree. See ADR-002 §6 item 6.
node-linker=hoisted
