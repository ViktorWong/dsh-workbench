{
  "name": "@dsh-workbench/panel-workbench",
  "version": "0.1.0",
  "description": "Workbench panel plugin for DeepSeek Harness",
  "license": "MIT",
  "type": "module",
  "main": "lib/index.mjs",
  "files": ["lib", "cordis.patch.yml"],
  "scripts": {
    "build": "tsdown src/index.ts --outDir lib --dts",
    "typecheck": "tsc --noEmit",
    "lint": "eslint .",
    "test": "vitest run",
    "pack": "pnpm pack"
  },
  "dependencies": {
    "@deepseek-ai/cordis": "^4.0.1"
  },
  "devDependencies": {
    "tsdown": "^0.22.0",
    "typescript": "^5.6.0",
    "vitest": "^4.1.0"
  },
  "dsh": {
    "bundle": {
      "patch": "./cordis.patch.yml"
    }
  }
}
